export const SEARCHING_ALGORITHMS_REGISTRY = {
  linear: {
    key: 'linear',
    name: 'Linear Search',
    cppCode: 'linear_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'cells',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(N)',
    worst: 'O(N)',
    space: 'O(1)',
    pseudocode: [
      'for i = 0 to n - 1:',
      '  if arr[i] == target: return i',
      'return -1'
    ],
    intuition: 'Scans array elements sequentially one by one from index 0 to n-1 until target is located.',
    mistakes: 'Using linear search on large sorted arrays where O(log N) Binary Search would be exponentially faster.',
    interviewTip: 'Requires zero preprocessing (no sorting needed) and works on unsorted streams or linked lists.'
  },
  sentinel: {
    key: 'sentinel',
    name: 'Sentinel Linear Search',
    cppCode: 'sentinel_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'cells',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(N)',
    worst: 'O(N)',
    space: 'O(1)',
    pseudocode: [
      'last = arr[n - 1], arr[n - 1] = target',
      'while arr[i] != target: i++',
      'arr[n - 1] = last',
      'if i < n - 1 or arr[n - 1] == target: return i'
    ],
    intuition: 'Places a sentinel target at the last index to eliminate loop boundary checking (`i < n`) on every iteration.',
    mistakes: 'Forgetting to restore the original value at the last array index after loop completes.',
    interviewTip: 'Saves 1 condition evaluation per iteration, reducing CPU branch instructions in inner loops.'
  },
  binary: {
    key: 'binary',
    name: 'Binary Search',
    cppCode: 'binary_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'bars_vertical',
    requiresSorted: true,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log N)',
    worst: 'O(log N)',
    space: 'O(1)',
    pseudocode: [
      'low = 0, high = n - 1',
      'while low <= high:',
      '  mid = low + (high - low) / 2',
      '  if arr[mid] == target: return mid',
      '  else if arr[mid] < target: low = mid + 1',
      '  else: high = mid - 1',
      'return -1'
    ],
    intuition: 'Repeatedly divides a sorted search interval in half by comparing the midpoint element to target.',
    mistakes: 'Integer overflow when calculating `mid = (low + high) / 2`; use `low + (high - low) / 2` instead.',
    interviewTip: 'Can be applied to any monotonic function search space, not just explicit memory arrays.'
  },
  recbinary: {
    key: 'recbinary',
    name: 'Recursive Binary Search',
    cppCode: 'recursive_binary_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'bars_vertical',
    requiresSorted: true,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log N)',
    worst: 'O(log N)',
    space: 'O(log N)',
    pseudocode: [
      'function recBinary(arr, low, high, target):',
      '  if low > high: return -1',
      '  mid = low + (high - low) / 2',
      '  if arr[mid] == target: return mid',
      '  else if arr[mid] < target: recurse(mid + 1, high)',
      '  else: recurse(low, mid - 1)'
    ],
    intuition: 'Recursive implementation of Binary Search dividing search intervals using recursive call frames.',
    mistakes: 'Missing base case boundary check leading to infinite recursion stack overflow.',
    interviewTip: 'Consumes O(log N) stack frames compared to O(1) space iterative Binary Search.'
  },
  jump: {
    key: 'jump',
    name: 'Jump Search',
    cppCode: 'jump_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'bars_vertical',
    requiresSorted: true,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(√N)',
    worst: 'O(√N)',
    space: 'O(1)',
    pseudocode: [
      'step = sqrt(n), prev = 0',
      'while arr[min(step, n) - 1] < target:',
      '  prev = step, step += sqrt(n)',
      'while arr[prev] < target:',
      '  prev++, if prev == min(step, n): return -1',
      'if arr[prev] == target: return prev'
    ],
    intuition: 'Jumps ahead by fixed block sizes of √N, then performs a linear search within the target block.',
    mistakes: 'Choosing arbitrary step sizes instead of optimal block size √N.',
    interviewTip: 'Outperforms Binary Search when jumping backwards is costly (e.g. tape drives or single-link systems).'
  },
  interpolation: {
    key: 'interpolation',
    name: 'Interpolation Search',
    cppCode: 'interpolation_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'bars_vertical',
    requiresSorted: true,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log log N)',
    worst: 'O(N)',
    space: 'O(1)',
    pseudocode: [
      'pos = low + ((target - arr[low]) * (high - low)) / (arr[high] - arr[low])',
      'if arr[pos] == target: return pos',
      'else if arr[pos] < target: low = pos + 1',
      'else: high = pos - 1'
    ],
    intuition: 'Estimates target position based on key value distribution, similar to looking up a word in a phone book.',
    mistakes: 'Applying to exponentially distributed datasets, degrading performance to O(N).',
    interviewTip: 'Achieves sub-logarithmic O(log log N) time complexity on uniformly distributed numerical data.'
  },
  exponential: {
    key: 'exponential',
    name: 'Exponential Search',
    cppCode: 'exponential_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'bars_vertical',
    requiresSorted: true,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log N)',
    worst: 'O(log N)',
    space: 'O(1)',
    pseudocode: [
      'if arr[0] == target: return 0',
      'i = 1',
      'while i < n and arr[i] <= target: i = i * 2',
      'return binarySearch(arr, i / 2, min(i, n - 1), target)'
    ],
    intuition: 'Finds range where target resides by exponentially doubling index bounds (1, 2, 4, 8...), then runs Binary Search.',
    mistakes: 'Forgetting to cap upper bound at array length `min(i, n - 1)`.',
    interviewTip: 'Particularly effective for unbounded or infinite arrays where length is unknown beforehand.'
  },
  fibonacci: {
    key: 'fibonacci',
    name: 'Fibonacci Search',
    cppCode: 'fibonacci_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'bars_vertical',
    requiresSorted: true,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log N)',
    worst: 'O(log N)',
    space: 'O(1)',
    pseudocode: [
      'fibM2 = 0, fibM1 = 1, fibM = fibM2 + fibM1',
      'while fibM < n: fibM2 = fibM1, fibM1 = fibM, fibM = fibM2 + fibM1',
      'offset = -1',
      'while fibM > 1:',
      '  i = min(offset + fibM2, n - 1)',
      '  if arr[i] < target: fibM = fibM1, fibM1 = fibM2, fibM2 = fibM - fibM1, offset = i',
      '  else if arr[i] > target: fibM = fibM2, fibM1 = fibM1 - fibM2, fibM2 = fibM - fibM1',
      '  else: return i'
    ],
    intuition: 'Divides sorted array into intervals using Fibonacci numbers, utilizing only addition and subtraction operations.',
    mistakes: 'Incorrectly updating Fibonacci variables during array range reduction steps.',
    interviewTip: 'Useful on hardware architectures where multiplication and division operations are costly.'
  },
  ternary: {
    key: 'ternary',
    name: 'Ternary Search',
    cppCode: 'ternary_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'bars_vertical',
    requiresSorted: true,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log3 N)',
    worst: 'O(log3 N)',
    space: 'O(1)',
    pseudocode: [
      'mid1 = low + (high - low) / 3',
      'mid2 = high - (high - low) / 3',
      'if arr[mid1] == target: return mid1',
      'if arr[mid2] == target: return mid2',
      'if target < arr[mid1]: high = mid1 - 1',
      'else if target > arr[mid2]: low = mid2 + 1',
      'else: low = mid1 + 1, high = mid2 - 1'
    ],
    intuition: 'Divides search space into three equal segments using two midpoints (`mid1`, `mid2`).',
    mistakes: 'Assuming Ternary Search makes fewer total comparisons than Binary Search (it actually performs more comparisons per step).',
    interviewTip: 'Frequently used to find unimodal function extrema (maximum or minimum).'
  },
  metabinary: {
    key: 'metabinary',
    name: 'Meta Binary Search',
    cppCode: 'meta_binary_search',
    category: 'Array Searching',
    viewType: 'array',
    defaultViewMode: 'bars_vertical',
    requiresSorted: true,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log N)',
    worst: 'O(log N)',
    space: 'O(1)',
    pseudocode: [
      'n_bits = lg(n)',
      'pos = 0',
      'for i = n_bits - 1 down to 0:',
      '  new_pos = pos | (1 << i)',
      '  if new_pos < n and arr[new_pos] <= target: pos = new_pos',
      'if arr[pos] == target: return pos',
      'return -1'
    ],
    intuition: 'One-sided binary search that evaluates indices bit-by-bit from most significant bit down to zero.',
    mistakes: 'Incorrectly setting initial bit length variable `n_bits`.',
    interviewTip: 'Ideal for memory systems with fast bitwise operations.'
  },
  hashtable: {
    key: 'hashtable',
    name: 'Hash Table Search',
    cppCode: 'hashtable_search',
    category: 'Hash Searching',
    viewType: 'hashtable',
    defaultViewMode: 'hashtable',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(1)',
    worst: 'O(N)',
    space: 'O(N)',
    pseudocode: [
      'hash_val = key % table_size',
      'bucket = table[hash_val]',
      'for item in bucket:',
      '  if item == target: return bucket_idx',
      'return -1'
    ],
    intuition: 'Maps key to hash bucket index via modulo hashing, enabling constant time average lookups.',
    mistakes: 'Poor hash function design leading to frequent bucket collisions.',
    interviewTip: 'Average O(1) lookup degradation occurs when collision resolution chains become long O(N).'
  },
  cuckoo: {
    key: 'cuckoo',
    name: 'Cuckoo Hash Search',
    cppCode: 'cuckoo_search',
    category: 'Hash Searching',
    viewType: 'hashtable',
    defaultViewMode: 'hashtable',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(1)',
    worst: 'O(1)',
    space: 'O(N)',
    pseudocode: [
      'pos1 = hash1(target), pos2 = hash2(target)',
      'if table1[pos1] == target: return pos1',
      'if table2[pos2] == target: return pos2',
      'return -1'
    ],
    intuition: 'Uses two hash functions and two tables, guaranteeing constant time O(1) worst-case search lookups.',
    mistakes: 'Infinite displacement loops during insertion; requires rehashing when cycles occur.',
    interviewTip: 'Guarantees O(1) worst-case lookup time because at most 2 locations are ever inspected.'
  },
  bst: {
    key: 'bst',
    name: 'Binary Search Tree Search',
    cppCode: 'bst_search',
    category: 'Tree Searching',
    viewType: 'tree',
    defaultViewMode: 'tree',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log N)',
    worst: 'O(N)',
    space: 'O(H)',
    pseudocode: [
      'curr = root',
      'while curr != null:',
      '  if curr.val == target: return curr',
      '  else if target < curr.val: curr = curr.left',
      '  else: curr = curr.right',
      'return null'
    ],
    intuition: 'Traverses binary tree branches left or right depending on node key comparison.',
    mistakes: 'Degrading to O(N) linear linked list on unbalanced skewed trees.',
    interviewTip: 'Balanced BST search maintains O(log N) operations.'
  },
  avl: {
    key: 'avl',
    name: 'AVL Tree Search',
    cppCode: 'avl_search',
    category: 'Tree Searching',
    viewType: 'tree',
    defaultViewMode: 'tree',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log N)',
    worst: 'O(log N)',
    space: 'O(log N)',
    pseudocode: [
      'curr = root',
      'while curr != null:',
      '  if curr.val == target: return curr',
      '  curr = (target < curr.val) ? curr.left : curr.right',
      'return null'
    ],
    intuition: 'Searches self-balancing binary search tree where height difference between left/right subtrees is at most 1.',
    mistakes: 'Confusing AVL tree height balance factor with Red-Black tree properties.',
    interviewTip: 'AVL trees are more rigidly balanced than Red-Black trees, providing faster lookups.'
  },
  redblack: {
    key: 'redblack',
    name: 'Red Black Tree Search',
    cppCode: 'redblack_search',
    category: 'Tree Searching',
    viewType: 'tree',
    defaultViewMode: 'tree',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(log N)',
    worst: 'O(log N)',
    space: 'O(log N)',
    pseudocode: [
      'curr = root',
      'while curr != null:',
      '  if curr.val == target: return curr',
      '  curr = (target < curr.val) ? curr.left : curr.right',
      'return null'
    ],
    intuition: 'Searches self-balancing tree using red/black node color invariants to maintain logarithmic height.',
    mistakes: 'Assuming color properties affect search traversal logic (search behaves identical to standard BST).',
    interviewTip: 'Red-Black trees require fewer rotations during insertion/deletion than AVL trees.'
  },
  trie: {
    key: 'trie',
    name: 'Trie Word Search',
    cppCode: 'trie_search',
    category: 'String Searching',
    viewType: 'trie',
    defaultViewMode: 'trie',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(L)',
    avg: 'O(L)',
    worst: 'O(L)',
    space: 'O(N * L)',
    pseudocode: [
      'curr = root',
      'for char in word:',
      '  if char not in curr.children: return false',
      '  curr = curr.children[char]',
      'return curr.isEndOfWord'
    ],
    intuition: 'Traverses prefix tree character-by-character along word nodes.',
    mistakes: 'Forgetting to check `isEndOfWord` flag when prefix matches a partial string.',
    interviewTip: 'Search time depends only on word length L, independent of total stored dictionary keys N.'
  },
  kmp: {
    key: 'kmp',
    name: 'KMP Pattern Search',
    cppCode: 'kmp_search',
    category: 'String Searching',
    viewType: 'pattern',
    defaultViewMode: 'pattern',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(N)',
    avg: 'O(N + M)',
    worst: 'O(N + M)',
    space: 'O(M)',
    pseudocode: [
      'lps = computeLPS(pattern)',
      'i = 0, j = 0',
      'while i < n:',
      '  if pattern[j] == text[i]: i++, j++',
      '  if j == m: return i - j',
      '  else if i < n and pattern[j] != text[i]:',
      '    if j != 0: j = lps[j - 1]',
      '    else: i++'
    ],
    intuition: 'Uses Longest Prefix Suffix (LPS) table to avoid re-examining matched characters upon mismatch.',
    mistakes: 'Incorrectly computing the LPS pre-processing array.',
    interviewTip: 'Guarantees O(N + M) worst-case time, avoiding naive O(N * M) string matching.'
  },
  rabinkarp: {
    key: 'rabinkarp',
    name: 'Rabin-Karp Pattern Search',
    cppCode: 'rabinkarp_search',
    category: 'String Searching',
    viewType: 'pattern',
    defaultViewMode: 'pattern',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(N + M)',
    avg: 'O(N + M)',
    worst: 'O(N * M)',
    space: 'O(1)',
    pseudocode: [
      'p_hash = hash(pattern), t_hash = hash(text[0..m-1])',
      'for i = 0 to n - m:',
      '  if p_hash == t_hash and text[i..i+m-1] == pattern: return i',
      '  t_hash = rehash(text, i)'
    ],
    intuition: 'Uses rolling hash algorithm to compare pattern hash with text window hashes.',
    mistakes: 'Not handling hash collisions with explicit character comparison.',
    interviewTip: 'Extremely efficient for multi-pattern matching applications.'
  },
  bfs: {
    key: 'bfs',
    name: 'Breadth First Search (BFS)',
    cppCode: 'graph_bfs',
    category: 'Graph Searching',
    viewType: 'graph',
    defaultViewMode: 'graph',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(V + E)',
    worst: 'O(V + E)',
    space: 'O(V)',
    pseudocode: [
      'queue = [start], visited = {start}',
      'while queue not empty:',
      '  node = queue.pop()',
      '  if node == target: return node',
      '  for neighbor in node.neighbors:',
      '    if neighbor not visited: visited.add(neighbor), queue.push(neighbor)'
    ],
    intuition: 'Explores graph level-by-level outward using a FIFO queue structure.',
    mistakes: 'Forgetting to mark nodes as visited when pushing to queue, causing infinite loops.',
    interviewTip: 'Guarantees finding the shortest path on unweighted graphs.'
  },
  dfs: {
    key: 'dfs',
    name: 'Depth First Search (DFS)',
    cppCode: 'graph_dfs',
    category: 'Graph Searching',
    viewType: 'graph',
    defaultViewMode: 'graph',
    requiresSorted: false,
    supportsComparison: true,
    best: 'O(1)',
    avg: 'O(V + E)',
    worst: 'O(V + E)',
    space: 'O(V)',
    pseudocode: [
      'stack = [start], visited = {}',
      'while stack not empty:',
      '  node = stack.pop()',
      '  if node not in visited:',
      '    visited.add(node)',
      '    if node == target: return node',
      '    for neighbor in node.neighbors: stack.push(neighbor)'
    ],
    intuition: 'Explores graph paths as deep as possible along each branch before backtracking using a LIFO stack.',
    mistakes: 'Stack overflow on deep recursion graphs.',
    interviewTip: 'Useful for topological sorting and connected components discovery.'
  }
};
